# Real secrets — never commit
.env

# Runtime data (orders, contact submissions) — generated by the server
data/orders.json
data/contacts.json
data/last-customer-email.html
data/*.json
!data/menu.json
!data/.gitkeep

# Dependencies
node_modules/

# Logs
*.log
npm-debug.log*

# OS junk
.DS_Store
Thumbs.db
